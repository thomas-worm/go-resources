{
	"name": "go-resources",
	"image": "mcr.microsoft.com/devcontainers/go",

	"features": {},

	"customizations": {
		"vscode": {
			"settings": {},
			"extensions": [
				"streetsidesoftware.code-spell-checker",
				"ms-vscode.makefile-tools"
			]
		}
	},
	"postCreateCommand": "sudo apt update && DEBIAN_FRONTEND=noninteractive sudo apt full-upgrade -y && go version"
}

